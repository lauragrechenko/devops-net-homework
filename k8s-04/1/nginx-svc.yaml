apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: http-nginx
      protocol: TCP
      port: 9001
      targetPort: 80
    - name: http-tool
      protocol: TCP
      port: 9002
      targetPort: 8080
